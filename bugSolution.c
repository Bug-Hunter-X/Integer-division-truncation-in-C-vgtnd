#include <stdio.h>

int main() {
  float x = 10.0; // Use floating point type for accurate division
  float y = 5.0; // Use floating point type for accurate division
  float z = x / y; 
  printf("%.1f\n", z); //Using %.1f to show floating point value
  return 0;
}